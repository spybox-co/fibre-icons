# Icons to optimizer

Path ```./opt/icons``` is suggested to use for non-optimized icons exported from Graphical Editor.
Use it to preparte icons for optimise them with SVG-opt script and run command:

```$ node export```
